kestra:
  repository:
    type: postgres
  queue:
    type: postgres
  storage:
    type: local
    local:
      base-path: /app/storage
  security:
    basic-auth:
      enabled: false
  flow-auto-loader:
    enabled: true
    paths:
      - /app/flows

datasources:
  postgres:
    url: jdbc:postgresql://postgres:5432/${KESTRA_DB_NAME:-kestra}
    driverClassName: org.postgresql.Driver
    username: ${KESTRA_DB_USER:-kestra}
    password: ${KESTRA_DB_PASSWORD:-kestra_local_password}
